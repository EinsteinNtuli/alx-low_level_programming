#include <stdio.h>
/**
 *main is the entry point
 *
 *the return is always 0 for a successful code
 */
int main (void)
{
	puts ("\"Programming is like building a multilingual puzzle");

	return (0);
}
