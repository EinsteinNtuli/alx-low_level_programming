#include <stdio.h>
/**
 * main-entry piont for the program
 *
 * Return: 0 if there are no errors, not zero if there are errors.
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
